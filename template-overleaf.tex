% !TEX TS-program = xelatex
% !TEX encoding = UTF-8 Unicode
%
% Overleaf 免上传字体体验入口。
% 本文件会让 template.tex 使用 LZUThesis_xb 的 overleaf 字体模式：
% - 不需要上传 SimSun.ttc、SimHei.ttf、SimFang.ttf、Arialbd.ttf 也能先编译。
% - 字体使用 TeX Live/Overleaf 通常自带的 Fandol 字体近似替代。
% - 正式提交前仍建议使用 template.tex，并按 fonts/README.md 准备规范字体。

\PassOptionsToClass{overleaf}{LZUThesis_xb}
\input{template.tex}
